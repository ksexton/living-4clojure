;; Intro to Strings - Elementary
;; Clojure strings are Java strings.  This means that you can use any of the Java string methods on Clojure strings.
;; tags - 
;; restricted - 
(ns living-4clojure.w1d1_p003
  (:use clojure.test))

(def __
;; your solution here
)

(defn -main []
  (are [soln] soln
(= __ (.toUpperCase "hello world"))
))
